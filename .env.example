# ===========================================
# Environment Variables for n8n GTM Workflow
# ===========================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to git.

# --- API Keys (set these in n8n Settings > Environment Variables) ---
ANTHROPIC_API_KEY=sk-ant-xxxxx
SIGNALHIRE_API_KEY=your-signalhire-api-key
REPLYIO_API_KEY=your-replyio-api-key

# --- SSH Tunnel (for Render deployment) ---
SSH_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----\n...\n-----END OPENSSH PRIVATE KEY-----"
SSH_HOST=your-server-ip
SSH_USER=your-ssh-user
DB_PORT=3306
LOCAL_PORT=3307

# --- Webhook Authentication ---
# Create an "Header Auth" credential in n8n with:
#   Header Name: X-Webhook-Secret
#   Header Value: (paste a strong random value below)
WEBHOOK_SECRET=generate-a-random-secret-here
